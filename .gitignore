# Python
.venv/
**/__pycache__/
**/*.pyc
**/*.pyo

# Secrets — never commit
.env
.streamlit/secrets.toml

# Local databases
# tracker.db holds real holdings — never committed
data/tracker.db
# demo.db is committed intentionally (paper trades for Streamlit Cloud)

# Generated reports — local artifacts, not committed
data/reports/

# Factor data cache — fetched at runtime from Ken French Data Library
data/ff_factors.csv

# Editor / tool dirs
.cursor/
.claude/
