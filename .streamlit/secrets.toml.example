# Copy this file's contents into Streamlit Cloud → App settings → Secrets.
# Do NOT commit secrets.toml itself (it is gitignored).

FRED_API_KEY = "your_fred_api_key_here"

# "demo" for the public Streamlit Cloud deployment
# "personal" for the private password-protected deployment
TRACKER_MODE = "demo"
