# Pinned exactly: the mobile stylesheet targets data-testid internals, so an
# unpinned Cloud rebuild onto a newer Streamlit could silently break styling.
streamlit==1.57.0
# Ranges, not pins: pandas 2.2.3 and 3.0.x (3.0.3 AND 3.0.5) were verified
# bit-identical on the 12-sleeve Brinson-Fachler reconciliation — full-coverage AND
# with the benchmark-coverage-gap renormalization branch deliberately forced into a
# state that changes the numbers — so there is no version-sensitive operation to pin
# against. The earlier "pandas 3 breaks BF" pin was a misattribution of a demo.db
# price-cache-leak symptom (see the CHANGELOG 2026-07-23 correction). The gap-branch
# value is now golden-pinned by
# tests/test_attribution.py::test_benchmark_gap_renormalization_is_numeric_stack_invariant,
# so a real numeric-stack regression fails loudly instead of being re-pinned blind.
# (.python-version stays 3.11: equivalence was proven there; 3.0.x ships cp314 wheels,
# so this relieves the 3.14 tension, but a 3.14 move is a separate, verified step.)
pandas>=2.2.0
numpy>=2.1.0
yfinance>=0.2.50
plotly>=5.24.0
# Pinned exactly: fredapi is a thin wrapper whose releases have changed return
# shapes before, and the Macro page's FRED series feed dated computations. An
# unpinned Cloud rebuild onto a new release could shift those silently — and the
# tests that would catch it are the live_data ones, excluded from the default
# suite, so CI would stay green while the deployed page went wrong.
fredapi==0.5.2
html5lib>=1.1
python-dotenv>=1.0.0
python-dateutil>=2.9.0
requests>=2.31.0
openpyxl>=3.1.0
jinja2>=3.1.0
weasyprint>=60.0
xhtml2pdf>=0.2.10; sys_platform == 'win32'
kaleido==0.2.1
lxml>=4.9.0
statsmodels>=0.14.0
scipy>=1.14.0
pytest>=8.3.0
